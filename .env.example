# OpenAI API Key (optional — app works without it using demo responses)
OPENAI_API_KEY=sk-your-key-here

# JWT Secret (change this in production!)
JWT_SECRET=nyay-mitra-jwt-secret-change-in-production

# Port (default: 5000)
PORT=5000
